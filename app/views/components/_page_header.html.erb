<%# Usage: render "components/page_header", title: "Vehicles", subtitle: "Browse all EVs" %>

<% title = local_assigns[:title] %>
<% subtitle = local_assigns[:subtitle] %>

<div class="mb-6 md:mb-8">
  <h1 class="text-2xl md:text-3xl lg:text-4xl font-bold"><%= title %></h1>
  <% if subtitle.present? %>
    <p class="text-base-content/70 mt-1 md:mt-2"><%= subtitle %></p>
  <% end %>
</div>
